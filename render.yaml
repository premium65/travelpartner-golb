services:
  - type: web
    name: travelpartner-api
    runtime: node
    rootDir: api-main
    buildCommand: echo "Build starting..."
    startCommand: node simple-server.js
    envVars:
      - key: NODE_ENV
        value: production
